#!/bin/sh
# Commit-message hook for the piccle engine.
# Activated through the repository-local core.hooksPath set by `cargo setup`.
# Bypass: git commit --no-verify

set -e
commit_msg_file="$1"

echo "+committed (commit message linter)"
committed --config .committed.toml --commit-file "$commit_msg_file"
